#!/usr/bin/env python
import sys
import urlparse

url = len(sys.argv) > 1 and sys.argv[1]
if not url:
    raise Exception('you need a graphite url bruv')

print urlparse.parse_qs(urlparse.urlparse(url).query)['target'][0]
